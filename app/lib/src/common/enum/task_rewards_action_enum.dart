/// {@template task_rewards_action_enum}
/// An enum that represents the actions that can be performed on both daily and weekly task rewards.
/// {@endtemplate}
enum TaskRewardsActionEnum {
  /// Add a daily or weekly task reward to local database.
  add,

  /// Delete a daily or weekly task reward from local database.
  delete,

  /// Update a daily or weekly task reward in local database.
  update,
}
